@model IEnumerable<Backoffice0._1.Models.C_grupo_productos_tipos>


    @if (Model != null)
    {
        foreach (var item in Model)
        {
            <img src="@Html.DisplayFor(modelItem=> item.icono_web)" style="width:75px;" onclick="ConsultaSubClases(@Html.DisplayFor(modelItem => item.id_grupo_producto_tipo))" class="img_icon" />
           
        }
    }

