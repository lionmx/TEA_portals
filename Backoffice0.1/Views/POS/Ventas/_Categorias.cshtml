@model IEnumerable<Backoffice0._1.Models.C_producto_tipo>
@using System.Web.Mvc.Html;


@if (Model != null)
{

    foreach (var item in Model)

    {
  
        <img src="@Html.DisplayFor(modelItem => item.icono_web)" class="img_icon" style="width:75px;" onclick="ConsultaSubcategorias(@Html.DisplayFor(modelItem => item.id_producto_tipo))" />
   
       



    }
}
