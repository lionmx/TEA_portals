@model IEnumerable<Backoffice0._1.Models.C_producto_tipo>
@using System.Web.Mvc.Html;


@if (Model != null)
{

    foreach (var item in Model)

    {

<button type="button" onclick="ConsultaSubcategorias(@Html.DisplayFor(modelItem => item.id_producto_tipo))" class="btn btn-default">@Html.DisplayFor(modelItem => item.nombre)</button>

    }
}