@model IEnumerable<Backoffice0._1.Models.Productos>
@using System.Web.Mvc.Html;

@if (Model != null)
{
    foreach (var item in Model)
    {
    <a onclick="Addcarrito('@item.c_productos_cat.nombre',@item.c_productos_precios.precio_publico, '@item.c_productos_sucursal.sku_producto',false,false,0,@item.c_productos_cat.id_producto)">
        <div class="col-md-55">
            <div class="thumbnail">
                <div class="image view view-first">
                    <!--if(@item.c_productos_cat.id_clase.HasValue)
                        {
                        <img style="width: 100%;height:90%; display: block;" src="@item.c_productos_cat.C_productos_clase alt="image" />
                        }-->
                    <img style="width: 100%;height:90%; display: block;" src="@item.c_productos_cat.C_producto_clasificacion.imagen_pequena" alt="image" />
                </div>
                <div>
                    <center><label style="color:black"><b>@Html.DisplayFor(modelItem => item.c_productos_cat.nombre)</b></label></center>
                    <!-- <label><strong>@Html.DisplayFor(modelItem => item.c_productos_precios.precio_publico)</strong></label> -->
                </div>
            </div>
        </div>
    </a>
    }
}