
@{ if (ViewBag.detallePedidosEntregando != null)
    {
        <table>
            @foreach (var n in ViewBag.detallePedidosEntregando)
            {
                <tr>
                    <td>@n</td>
                </tr>
            }

        </table>

    }

}